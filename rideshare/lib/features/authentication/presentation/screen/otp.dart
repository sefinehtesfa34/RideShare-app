import 'package:flutter/material.dart';

class otpPage extends StatelessWidget {
  const otpPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(child: Text("Verification")),);
  }
}